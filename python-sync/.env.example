TR_PHONE_NUMBER=+4915112345678
TR_PIN=1234

# Supabase — MUST be the service role key, not the anon key, so this script can write
# regardless of RLS policies. Dashboard -> Project Settings -> API -> "service_role" key.
# This key bypasses RLS entirely — keep it out of the Next.js app and out of git.
SUPABASE_URL=SUPABASE_URL
SUPABASE_SERVICE_ROLE_KEY=SUPABASE_SERVICE_ROLE_KEY
